import OverviewWorkspace from "@/components/instrument/OverviewWorkspace";
// Instrument Mode: owns the viewport, no Workbench furniture.
export default function Page() { return <OverviewWorkspace />; }
