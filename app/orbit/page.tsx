// DEVELOPMENT ROUTE. Orbit's foundation pass — the read model made visible
// so its structure can be reviewed. Deliberately absent from DESTINATIONS:
// it is not an approved instrument and must not appear in the rail beside
// ones that are. Reachable by typing the URL, which is all this pass needs.
import OrbitPageClient from "@/components/OrbitPageClient";
export default function Page() {
  return <OrbitPageClient />;
}
