import ComingNext from "@/components/ComingNext";

export default function ForecastPage() {
  return (
    <ComingNext
      title="Forecast"
      description="Likely, earliest, and latest release dates — simulated from Linear estimates plus the effort implied by open findings."
    />
  );
}
