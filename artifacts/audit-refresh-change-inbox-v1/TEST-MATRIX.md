# Test Matrix

| Case | Expected proof | Automated coverage |
| --- | --- | --- |
| A | New knowledge/no Reality delta becomes one information-only receipt | DB proof |
| B | Scope cut routes to Capability owner and recomputes Forecast state | DB proof |
| C | Contradiction creates one OPEN Decision with two directions and no gate | DB proof |
| D | Occurred milestone creates typed Timeline event | DB proof |
| E | Stale blocker resolves existing Finding without duplication | DB proof |
| F | Source health remains a setup card and rejects canonical acceptance | DB proof |
| G | JSA bleed, KIT Construct context, and real Platform dependency classify correctly | Pure + DB proof |
| H | Hermes `ingesting` waits and creates no partial refresh | Pure proof |
| I | Current recent package creates no expensive rescan or fake delta | Pure proof |
| J | Retry/double-click converges on one canonical object | DB proof |
| K | Reality revision invalidates and derived revision catches up automatically | DB proof |
| L | Report readiness blocker census changes as blockers clear | DB proof |

## Regression result

The requested disposable PostgreSQL migration pass applied all 24 migrations from an empty database. TypeScript, production build, DateOnly/Instant, Production Hardening 2, activation/companion, 189/189 project-context matrix, Reports fixtures/composer/persistence/DB, protected Forecast/capacity, protected Audit 438/543, Rubric 129/129, Phase 3A/3B, Search, interaction/layout, and production-mode browser smoke passed.

Repository-wide lint reports zero errors and 44 warnings that already exist outside this change; targeted lint over every changed TypeScript/TSX surface reports zero warnings. Two legacy proof blocks requiring private, uncommitted production-capture inputs were unavailable; their checked-in protected alternatives passed. Exact results are in `measurements.json`.
