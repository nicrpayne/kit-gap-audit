# Search and Trace acceptance

## Search

Signal MiniSearch remains lexical/tokenized/fuzzy and runs behind the Rubric menu. Production-scale benchmark: 429 documents, 10.59 ms document+index build, 0.239 ms median query and 1.338 ms p95.

| Query | Top result | Result |
|---|---|---|
| `JSA Notifications Discussion` | `source:pkg:ke://source/transcript/2026-08-19_KE-JSA-Notifications-Discussion` | PASS |
| `KE Dev Standup` | 2026-08-25 transcript ahead of 2026-08-11 tie | PASS |
| `notifictions` | notification passage/transcript/external decision | PASS |
| exact notification quote | `passage:snap-jsa-1:ke-ev-0132` | PASS |
| `SOF-487` | `work:SOF-487` only | PASS |
| Decision title | `decision:d-batching` | PASS |
| `unbounded tail risk` | external risk Attention echo, canonical ID preserved | PASS |
| `Notion` | Notion lane, then real declared/package sources | PASS |

Selecting a result invokes Rubric fly-to and popup. Clearing Search restored camera, layout and selection exactly. Twenty-query disclosure proofs show no cumulative world mutation.

## Trace

Verified canonical paths:

- Source → passage → Finding → Evidence lane → Reality
- Passage → Finding → Evidence lane → Reality
- Search-selected external Attention echo → its canonical six-edge provenance path
- Passage connection row → source transcript through Rubric selection/fly-to

The full source path uses only `extracted_from`, `evidenced_by`, `concerns` and `supports`. The overlay uses live Rubric positions and retains dimmed world context.

Acceptance found and fixed two veracity defects:

1. Starting Search now clears the active Trace before showing unrelated results.
2. Presentation echoes now reference the exact existing canonical Trace path; no relationship is synthesized.

Work item `SOF-487` correctly offers no Trace because the current canonical provenance traversal contains no supported path to Reality for that object.

