# Recommended next phase — do not implement yet

## Gate before any new tranche

Complete the one credential-backed acceptance run using the command in `AUDIT-LIFECYCLE.md`. Promotion review should see a real AuditRun, new Findings, same-world refresh, History entry and unchanged Reality census.

## Phase 3C proposal: Audit sensemaking and navigation

1. **Complete Project Overview.** Add canonical, source-coverage, grounding and current/prior metrics from existing read models, with fly-to only where a canonical target exists.
2. **Alternate-layout label polish.** Use Rubric's existing label budget/progressive identity thresholds to reduce Circle/Hex collision; do not alter coordinates or physics.
3. **Deep Source Navigation.** Add provider resolvers and source/artifact drill-down only from typed resolver capabilities; never infer a URL.
4. **Current-versus-prior visual comparison.** Express added/changed/resolved Audit objects as material state over the existing Rubric world, preserving camera and canonical identity.
5. **Hybrid Search plan.** Layer exact identity → current MiniSearch → semantic retrieval → graph-aware expansion. Every result must state `Exact match`, `Text match`, `Semantic match`, or `Canonical connection`; semantic similarity must never become an edge.

This phase should not refine disagreement distance until the above workflow surfaces are complete and the model-backed lifecycle is proven.

