# Scope

The canonical read returns zero executable JSA work items in the production-shaped fixture. That is a coverage state, not evidence that no work exists.

The empty state now answers:

- represented: zero canonical executable items and zero modeled executable days;
- not represented: one open missing-work Audit Finding, not promoted into Reality;
- provider: the Linear owner read and its as-of date/availability;
- forecast explanation: legacy inferred capacity and declared dependencies/gates can still determine landing when Scope effort is zero.

The release card therefore never uses `0.0d` as a claim of completeness.
