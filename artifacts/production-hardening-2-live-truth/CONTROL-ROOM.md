# Control Room

The header distinguishes `Last saved forecast report` from the Forecast owner's source age. The Likely Outcome tile uses amber when the owner read is stale and prints the full date plus `Live owner · Stale 31d · as of Aug 5`.

Decision totals use the lifecycle/gating-subset contract. Constraint rows separate magnitude, unit, subject, and target project. What Changed separates the event title from its fixed age column and clamps long titles to two lines. The screen's structure and interaction model are unchanged.
