# Capacity

Three independent readings are now named:

1. Forecast basis — the capacity used by the current baseline Forecast.
2. Named allocations — people represented in Portfolio staffing and available to its mixer.
3. Scenario input — the current what-if value; it does not silently replace the baseline.

Fixture result: `Forecast basis: 1.0 FTE · legacy inferred · unreconciled`; `Named allocations: 0.0 FTE · not configured`; `Scenario input: 0.0 FTE`.

No phantom named person or allocation is created. Existing FTE conservation and commit guards are unchanged.
