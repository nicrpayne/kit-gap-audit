# Project Activation Phase 2 evidence bundle

This bundle records the synthetic, disposable proof of the complete governed activation boundary. No production data is included.

Production authority was checked through the live `/api/version` endpoint and origin. The branch was created directly from Phase 1 SHA `9153ee741f3a8de8d2fb93e4c52a31a60a074fe5`, whose ancestry is the current production baseline `bce38dde332fa1d049363fe502c5921326556311` plus three isolated Phase 1 commits. No production branch was changed and nothing was deployed.

The database proof covers rich, sparse, no-execution, contradictory, rejected, manual assertion, Decision-vs-Gate, response-loss retry, concurrent retry, and forced-validation rollback cases. The Hermes bridge proof additionally covers pre-Scope identity selection, deterministic v1.1 compilation, current-head currency, one-hop typed graph retention, linked raw evidence, derivative wiki lineage, typed-only dependencies, and the explicit absence of semantic retrieval and geometry. `measurements.json` is the machine-readable census; screenshots are non-write browser captures of the synthetic workflow.
