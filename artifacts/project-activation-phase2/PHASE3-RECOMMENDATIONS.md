# Phase 3 recommendations

1. Configure a reviewed Git remote for the proved standalone `kit-gap-bridge` `0.2.0` commit, publish an immutable release artifact/tag, and document operator install/upgrade/rollback. Preserve its pre-Scope identity selection and typed-only dependency rule.
2. Add production-ready semantic retrieval only behind explicit coverage/health reporting; never expose similarity as confidence or causal truth.
3. Add governed post-activation candidate re-acceptance and Reality diffs.
4. Add merge ancestry only with durable parent candidate IDs, disposition/evidence lineage, and stable merged fingerprints.
5. Extend the Audit model beyond deterministic first-pass rules while preserving evidence-bound findings.
6. Finish `Add Project → Report → ChatGPT Site` publishing after the lifecycle has production review.
