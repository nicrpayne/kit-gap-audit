# Decision / JSA → KIT Construct pivot brief

The Decision / Scenario recipe is implemented, but the known pivot remains an honest incomplete brief.

Available today:

- JSA LIVE Forecast, target, Scope, open first-class Decisions/Gates, dependencies, Audit evidence/currentness, and existing JSA scenarios.

Missing today:

- canonical KIT Construct project world and Scope;
- named/reconciled allocations when production has none;
- support-tail and stranded/deferred-work contracts;
- complete Forecast-owned pivot options and their date/window/confidence consequences.

Therefore Reports cannot yet answer “is JSA done enough,” “who moves at what FTE,” or recommend finish-first/staged/immediate pivot without fabrication. The fixture and proof require `KIT_CONSTRUCT_MISSING` and `CAPACITY_MISSING` to remain visible.
