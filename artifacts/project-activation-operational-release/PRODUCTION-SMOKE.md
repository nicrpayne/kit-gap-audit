# Production smoke

Status at evidence-set creation: controlled promotion pending.

The production proof will use a clearly synthetic pre-Reality bootstrap. It
will verify automatic companion pickup, Review population, provenance, zero
canonical writes, unaffected existing project context, and read-only loading of
Control Room, Audit, Decisions, Forecast, Portfolio, Scope, Dependencies,
Timeline, and Reports. It will not activate the synthetic bootstrap because no
governed safe delete path exists, and it will not run Audit or create governed
records for an existing project.

This file is updated by a docs-only forward commit after the first deployed
code marker is smoke-tested; the final marker is then verified again.
