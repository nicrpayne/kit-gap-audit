# Merge and replay resolution

## Textual conflicts

### `components/audit/AuditInstrument.tsx`

Resolution retained the truth-hardened canonical boundaries (`dispatchFindingAction`, canonical identifiers, source resolvers, `SignalLink`, presentation-only hiding) and applied the accepted floating Inspector, Trace continuity, second-level review and widget material around them. The source branch's provisional Reality glyph was removed so the production center identity remains authoritative.

### `components/audit/CanvasAuditRenderer.tsx`

Resolution retained truth-hardened hidden-ID projection behavior and the full current Rubric layout set (`rings`, `circle`, `hex`, `force`) while applying the accepted Inspector/widget control contract. No node position, camera, drag, morph or topology rule was replaced.

## Architectural conflict

The source branch was cut from `edf3dfff8e952ac9c12f0c8151779f0963477292`, where the legacy `AuditInstrument` was the demonstrated surface. On the truth-hardened base, production `/audit` mounts `AuditWorld` and embeds the protected Rubric Phase 3 world. Replaying the old tree alone would therefore have produced dead UX code.

The accepted behavior was ported to the active boundaries:

- `AuditWorld.tsx` — parent shell, Project Overview handoff and governed review sheet.
- `phase2-host.js` — canonical Search/Trace/action behavior.
- `phase3-host.js` — Inspector lifecycle and inspectable camera/selection state.
- `rubric-phase3/route.ts` — embedded widget material only.

The protected Phase 1 files were not edited. No source evidence commit from the stale mount was reused as current acceptance evidence.
