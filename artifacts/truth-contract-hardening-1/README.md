# Signal Truth Contract Hardening 1

Date: 2026-09-04 (America/Chicago)

## Identity and safety

- Starting production SHA: `95407ec18fc2b4e6d8e8fe3892975606dd0607fd`
- Production branch: `claude/product-timeline-audit-a72dmg`
- Deployment: `3ec9bd20-d702-41e2-a15d-6e8a38427cd2`
- Experimental branch: `codex/truth-contract-hardening-1`
- Production activity: read-only route and browser-console smoke only
- No production Audit, Finding action, Decision action, allocation action, Timeline write, or Reality mutation occurred.
- Protected Monte Carlo files changed: none.
- Protected Rubric files changed: none; byte-equivalence proofs pass.

## Result

The five verified truth boundaries are implemented and covered by one deterministic owner/consumer proof:

1. Open Findings remain Audit observations and add zero baseline work. A governed Linear representation enters Forecast once through the canonical issue path. The Linear read cache is invalidated after a successful filing so there is no temporary missing-work gap.
2. A shared internal-link contract carries `project`, supported selected-object keys, and explicit scenario keys through rail, command-menu, and in-instrument handoffs. Decisions restores URL-selected canonical objects. Timeline error/loading states keep the shell and its context-preserving navigation.
3. Decisions API joins the target Scope through `DecisionGate.targetScopeId`; the circuit and inspector render that target rather than the Decision's home Scope.
4. Timeline uses an explicit `live | historical` forecast-reading union. At NOW it accepts only current Forecast output; in history it accepts only immutable Report memory. Missing live data does not fall back to an old report.
5. Named capacity has a machine-checkable contract from Person/Allocation through raw FTE, context-switch-adjusted effective FTE, and the exact Forecast input. Legacy inferred scopes remain operational but are explicitly classified as unreconciled, unstaffed capacity rather than roster-backed fact.

## Secondary Scope result

The production-shaped Platform fixture has 17 remaining rows and 44 likely days before hierarchy handling. `SOF-810` contributes 3 days and is the fetched parent of `SOF-487`. The executable-leaf rule excludes a parent only when another counted row names it as parent, yielding 41 likely days. A dangling parent such as `SOF-747` remains counted because no fetched descendant proves that its work is represented elsewhere.

## Automated acceptance

- `npx tsx scripts/truth-contract-hardening-proof.ts`: PASS
- `npx tsc --noEmit`: PASS
- `npm run lint -- --max-warnings=9999`: PASS, 0 errors; existing warning debt only
- `npm run build`: PASS
- Signal Rubric adapter veracity: PASS, protected files byte-identical
- Signal Rubric Phase 3A: PASS
- Signal Rubric Phase 3B: PASS
- Orbit canonical graph proof: PASS
- Local browser route/context matrix: PASS for Control Room, Audit, Decisions, Forecast, Portfolio/Capacity, Scope, Dependencies, Timeline, and Reports shell
- Local Back/Forward context restoration: PASS
- Production route smoke: PASS for every instrument and Reports shell; build marker `95407ec`; no captured browser warnings/errors

## Current production data caveats

- Current production still has zero named allocations while Forecast uses legacy inferred capacity for JSA and Platform. The code now names this as unreconciled rather than allowing it to masquerade as named staffing. Production data must be allocated before a JSA → KIT Construct staffing statement is trustworthy.
- KIT Construct has no canonical Scope/work/capacity world in the tested production data, so a real pivot option comparison still cannot be produced.
- Audit source staleness, unsupplied providers, weak passage grounding, and suspicious test records remain governed data-hardening work; none were silently altered.

## Evidence

- Permanent proof: `scripts/truth-contract-hardening-proof.ts`
- Reports upstream contract: `REPORTS-UPSTREAM-CONTRACTS.md`
- Measurements: `measurements.json`
- Production baseline screenshot: `screenshots/production-control-room-95407ec.png`

