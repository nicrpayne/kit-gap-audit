# Truth and scope checks

| Gate | Result |
|---|---|
| Canonical Signal node IDs | Unchanged; 427/427 projected by ID |
| Graphology graph | Unchanged; renderer proof compares order, size, node attributes, and absence of geometry writes |
| Canonical edges | 439/439 source relationships preserved; runtime creates no link force and no new canonical edge |
| Reality | No new mutation path; review still uses existing explicit human-confirmed actions |
| Disagreement | Only 0/6/12 radial offset inside Memory; trust/currentness/provenance do not add distance dimensions |
| Source lineage | Provider comes from canonical kind/provider metadata; projected child ownership inherits through its real local parent only |
| Source actions | `Open source` requires a real `http:`/`https:` value. No URL is synthesized. `Open on device` remains unavailable without a trusted resolver. |
| Hide | Reversible presentation-only set; no canonical delete/remove action exists |
| Search | MiniSearch remains client-local; temporary disclosure clears exactly; identity result outranks locating prose in the exercised query |
| Trace | Uses `traceIsComplete` and canonical supported relations only |
| Other instruments | No Control Room, Forecast, Portfolio, Scope, Decisions, Dependencies, Timeline, Reports, Scenario, API, DB, or shared product file changed |

## Remaining divergences

- The fixture carries no safe browser URL or trusted local-device resolver, so those conditional card actions cannot be demonstrated without fabricating authority.
- Signal's denser domain labels create more central label pressure than Rubric's filesystem demo; collision policy remains Signal's explicitly approved replacement area from the handoff.
- The database-backed proof remains environment-blocked by the absent `DATABASE_URL`; all DB-free truth checks pass.

